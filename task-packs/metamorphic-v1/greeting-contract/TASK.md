# Implement a greeting command

Implement the configured greeting command. It must print the configured prefix, one space, the single provided name, and a newline. Any argument count other than one must fail with a `usage:` diagnostic on stderr.
