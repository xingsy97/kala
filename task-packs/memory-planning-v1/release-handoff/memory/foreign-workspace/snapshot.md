# Unrelated workspace snapshot

This record is not part of the release task. Its owner is `green-team`, its region is `eu-west-1`, and its package format is `zip`. These values must remain isolated from the release handoff.
