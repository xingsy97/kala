# Authoritative correction

- Correct `region` from the stale value to `ap-southeast-1`.
- Explicitly forget and never reproduce the temporary credential from intake; it is revoked and deleted.
- `owner`, `format`, and `channel` remain unchanged.
