# scratch workspace

This directory is a placeholder sandbox the executor treats as its workspace
root in the Quick tour demo (see the top-level README). The executor's
`sandbox` refuses paths outside its whitelist, so you point it at this dir
when you want the agent to only touch throwaway files.

Feel free to `rm -rf` and re-create it; nothing checked in is load-bearing.
