#!/usr/bin/env node
import puppeteer from 'puppeteer-core'
const email=process.env.SAAS_TEST_EMAIL?.trim();let password=process.env.SAAS_TEST_PASSWORD?.trim();if(!email||!password)throw new Error('SAAS_TEST_EMAIL and SAAS_TEST_PASSWORD required')
const browser=await puppeteer.launch({executablePath:process.env.CHROME_PATH??'/snap/bin/chromium',headless:true,acceptInsecureCerts:true,args:['--no-sandbox']});let failed=false
const clickContinue=async p=>{await p.waitForFunction(()=>[...document.querySelectorAll('button')].some(b=>b.textContent?.trim()==='Continue'&&!b.disabled));await p.$$eval('button',bs=>bs.find(b=>b.textContent?.trim()==='Continue'&&!b.disabled)?.click())}
const login=async p=>{await p.goto('http://localhost:13001/auth/login',{waitUntil:'networkidle2'});await p.type('input[name=loginName]',email);await clickContinue(p);await p.waitForSelector('input[name=password]');await p.type('input[name=password]',password);await clickContinue(p);await new Promise(r=>setTimeout(r,800));if(await p.$('input[name=currentPassword]')){const next=`${password}N2!`;await p.type('input[name=currentPassword]',password);await p.type('input[name=password]',next);await p.type('input[name=confirmPassword]',next);await clickContinue(p);password=next}await p.waitForFunction(()=>location.origin === 'http://localhost:13001',{timeout:60_000});await p.waitForSelector('[data-testid=app-shell-nav]',{timeout:60_000})}
for(const scenario of [{name:'desktop',viewport:{width:1440,height:900}},{name:'mobile',viewport:{width:390,height:844,isMobile:true,hasTouch:true,deviceScaleFactor:2}}]){const c=await browser.createBrowserContext(),p=await c.newPage();await p.setViewport(scenario.viewport);const errors=[],bad=[];p.on('pageerror',e=>errors.push(String(e)));p.on('console',m=>{if(m.type()==='error')errors.push(m.text())});p.on('response',r=>{if(r.status()>=400)bad.push({status:r.status(),url:r.url()})});await login(p);const pages=['agent','operations','artifacts','pipeline','docs'];const report={scenario:scenario.name,pages:{},errors,bad};for(const page of pages){await p.click(`[data-testid=app-shell-nav-${page}]`);await new Promise(r=>setTimeout(r,1200));report.pages[page]=await p.evaluate(()=>({text:document.body.innerText.slice(0,500),overflow:document.documentElement.scrollWidth>innerWidth+1,loading:[...document.querySelectorAll('[role=status]')].map(n=>n.textContent?.trim()).filter(Boolean)}));await p.screenshot({path:`/tmp/saas-${scenario.name}-${page}.png`})}const benchmark=await p.$('[data-testid=app-shell-nav-benchmarks]');report.benchmarkVisible=!!benchmark;console.log(JSON.stringify(report));if(errors.some(x=>!x.includes('favicon.ico'))||bad.some(x=>!x.url.includes('favicon.ico'))||benchmark||Object.values(report.pages).some(x=>x.overflow))failed=true;await c.close()}await browser.close();process.exit(failed?1:0)
