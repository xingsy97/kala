# @agent-kernel/eval-agent-codex

Codex app-server backend plugin for the Agent Evaluation Platform. It exports
`CodexAgentBackend`, `createCodexAgentBackend`, `evaluationPlugins`, and the
`agent-eval-codex-app-server` driver.

The Codex executable and credentials are operator-supplied runtime dependencies.
