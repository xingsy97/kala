# @agent-kernel/eval-agent-claude-code

Claude Code CLI backend plugin for the Agent Evaluation Platform. It normalizes CLI
events through the public Eval SDK and exports `ClaudeCodeAgentBackend`,
`createClaudeCodeAgentBackend`, and `evaluationPlugins`.

The Claude Code executable and credentials are operator-supplied runtime dependencies.
