# @agent-kernel/eval-agent-custom-command

Non-ranked deterministic command Agent used for development and deployment
acceptance. It exports `CustomCommandAgentBackend`,
`createCustomCommandAgentBackend`, and `evaluationPlugins`. It is not an official
ranked Agent implementation.
