# @agent-kernel/eval-agent-cli-common

Shared public utilities for structured CLI Agent adapters. The package exports the
`StructuredCliAgentBackend` base class plus normalized-event and credential mapping
helpers. It does not select or install an Agent executable.
