# @agent-kernel/eval-environment-lxd-container

LXD container sandbox-provider plugin. It exports `createLxdContainerProvider` and
`evaluationPlugins`. A configured LXD environment is an operator-supplied runtime
dependency.
