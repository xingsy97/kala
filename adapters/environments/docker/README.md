# @agent-kernel/eval-environment-docker

Docker sandbox-provider plugin for isolated evaluation trials. It exports
`DockerSandboxProvider`, `createDockerProvider`, and `evaluationPlugins`. A compatible
Docker daemon is an operator-supplied runtime dependency.
