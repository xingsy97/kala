# @agent-kernel/eval-environment-lxd-vm

LXD virtual-machine sandbox-provider plugin. It exports `createLxdVmProvider` and
`evaluationPlugins`. A configured LXD environment with VM support is an
operator-supplied runtime dependency.
