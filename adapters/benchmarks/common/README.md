# @agent-kernel/eval-benchmark-common

Shared public primitives for declarative benchmark adapters. It exports
`DeclarativeBenchmarkAdapter`, policy and verification types, metric helpers, and
`createDeclarativeBenchmarkPlugin`.
