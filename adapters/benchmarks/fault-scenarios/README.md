# @agent-kernel/eval-benchmark-fault-scenarios

Deterministic fault-scenario benchmark plugin and metrics for recovery behavior. It
exports `evaluationPlugins`, `createFaultScenarioAdapter`, and
`faultScenarioMetrics`.
