# @agent-kernel/eval-benchmark-memory-planning

Memory and planning benchmark plugin with deterministic observation parsing,
execution-order derivation, and metrics. It exports `evaluationPlugins`, the adapter
factory, public observation types, and metric helpers.
