# @agent-kernel/eval-benchmark-program-bench

ProgramBench-compatible declarative benchmark plugin. It exports
`evaluationPlugins`, `createProgramBenchAdapter`, and `programBenchMetrics`. Dataset
licensing and task acquisition remain operator responsibilities.
