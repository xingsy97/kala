# @agent-kernel/eval-benchmark-custom-task-pack

Adapter for public custom task packs. It exports `CustomTaskPackAdapter`,
`createCustomTaskPackAdapter`, and `evaluationPlugins`. External task packs should
use the templates and contracts from `@agent-kernel/eval-sdk`.
