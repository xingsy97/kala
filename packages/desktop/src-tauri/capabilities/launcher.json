{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "launcher",
  "description": "Only the packaged launcher can connect. Dashboard UI hints use a separate origin-bound capability.",
  "local": true,
  "windows": ["launcher"],
  "permissions": ["allow-connect", "allow-window-control"]
}
