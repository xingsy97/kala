{
  "name": "@agent-kernel/desktop",
  "version": "0.2.0-rc.7",
  "private": true,
  "type": "module",
  "description": "Linux-only Tauri client for an existing Agent RunLab Dashboard",
  "scripts": {
    "build": "node ../../scripts/release/build-desktop-release.mjs",
    "dev": "tauri dev",
    "test": "node --test tests/*.test.mjs",
    "test:native": "cd src-tauri && cargo test --locked --release"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
