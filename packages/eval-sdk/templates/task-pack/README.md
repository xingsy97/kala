# Sample task-pack adapter

This starter resolves public synthetic tasks and preserves every declared verification step as native evidence. Replace the fixture archive reference and hashes with immutable, distributable assets. Publication requires explicit license, evaluation permission, provenance, and verifier versions.
