# Sample defect detector

This starter flags explicit error events in canonical analyzer input. A production detector must ship versioned positive and negative corpora, publish precision/recall, use immutable evidence references, and keep human validation separate from automatic detection.

Load the built module using agent-eval-analyzer --plugin @example/eval-detector-sample or AGENT_EVAL_ANALYZER_PLUGINS=@example/eval-detector-sample.
